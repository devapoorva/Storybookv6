import AppCrousel from "./AppCrousel"

export default {
  title: "Components/Crousel",
  component: AppCrousel
}

const Template = args => <AppCrousel {...args} />

export const Crousel = Template.bind({})
// Crousel.args = {
    
// }